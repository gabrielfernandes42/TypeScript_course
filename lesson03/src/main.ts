let myName: string;
let meaningOfLife: number;
let isLoadingL: boolean;
let album: any;

myName = "gabriel";
meaningOfLife = 42;
isLoadingL = true;
album = "qualquer coisa por aqui";

const sum = (a: number, b: string) => {
  return a + b;
};

let postID: string | boolean | number;
let isActive: number | boolean;

let re: RegExp = /\w+/g;
